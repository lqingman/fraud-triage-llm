# Full reference of every dependency across all phases.
#
# For local work on Windows/macOS, do NOT install this file directly:
# `vllm` (no Windows wheel) and `bitsandbytes` (no reliable Windows wheel)
# will fail. Instead install per environment:
#   - local (Phase 0 data, Phase 2 eval):  pip install -r requirements-base.txt
#   - Kaggle/Colab GPU training (Phase 1):  pip install -r requirements-train.txt
#   - serving image (Phase 4, Docker):      pip install -r requirements-serve.txt

# --- Core fine-tuning (Phase 1) ---
transformers>=4.44
peft>=0.12
bitsandbytes>=0.43
accelerate>=0.33
datasets>=2.20
trl>=0.9
# unsloth   # optional: faster QLoRA; install per their platform-specific instructions

# --- Schema / validation ---
pydantic>=2.7

# --- Eval (Phase 2) ---
scikit-learn>=1.5
xgboost>=2.1          # classical baseline for comparison
pandas>=2.2
numpy>=1.26

# --- ASR frontend (Phase 3) ---
faster-whisper>=1.0   # off-the-shelf; we do NOT train this

# --- Serving (Phase 4) — fastapi/httpx are cross-platform, CPU-only, and
# unit-tested without a GPU; uvicorn/vllm/gradio need Linux+GPU to actually
# run (see requirements-serve.txt) ---
fastapi>=0.111
httpx>=0.27
python-multipart>=0.0.9
uvicorn[standard]>=0.30
vllm>=0.5             # GPU inference server
gradio>=4.0           # demo UI

# --- Experiment tracking ---
wandb>=0.17
mlflow>=2.14

# --- Dev ---
pytest>=8.0
pyyaml>=6.0
